<!DOCTYPE html>
<html>
    <head>
        {{template "base/head" .}}
       <title>{{.RoomID}}聊天室--WebSocket</title>
    </head>
    <body>
        <div class="container">
    <h3>用户名: <span id="uname">{{.UserName}}</span></h3>
    <h3>聊天室: <span id="roomid">{{.RoomID}}</span></h3>
    <h4>发件箱:</h4>
    <form class="form-inline">
        <div class="col-md-6 form-group">
            <input id="sendbox" type="text" class="form-control" onkeydown="if(event.keyCode==13)return false;" required>
        </div>
        <button id="sendbtn" type="button" class="btn btn-default">发送</button>
    </form>
</div>

<div class="container">
    <h3>消息记录:</h3>
    <ul id="chatbox">
        <li>欢迎</li>
    </ul>
</div>
        {{template "base/footer" .}}
    </body>
</html>
